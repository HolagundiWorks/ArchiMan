<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Exports are deliberately staged in private cache. Never expose the
         database, managed measurement photos, drafts, or external storage. -->
    <cache-path name="export_cache" path="." />
</paths>
